# Reference for a GROMOS input style the original suite did not cover
# (see crates/gromos-md/tests/gromosXX_references/README-livecoms.md).
# gromosXX v1.6.0, double precision, 72 atoms

[system]
name = "aladip_multibath_collapsed"
description = "The same run with the blocks wrapped as real GROMOS inputs write them (one line)"
level = 5
natoms = 72
isolates = "value-stream IMD parsing: NLRELE on the APPAK line, TOLA2=1e-10, CONSTRAINT on one line"

[input]
topology = "../shared/aladip.topo"
configuration = "../shared/aladip.conf"
parameters = "aladip_multibath_collapsed.in"

[expected]
forces = "expected/forces.trf"
energies = "expected/energies.tre"
trajectory = "expected/trajectory.trc"
final_conf = "expected/final.conf"

[tolerances]
force_abs = 1e-6
energy_rel = 1e-8
position_abs = 1e-9
